function Page_StateAndReactivity(params){const _root=document.createDocumentFragment();const _e727=WF.el("div",{className:"wf-stack wf-gap--lg"});const _e728=WF.el("div",{className:"wf-markdown",markdown:"WebFluent's reactivity is fine-grained: a `state` is a signal, and anything that reads it — a text, an attribute, a `derived` value, an `effect`, a style splice — follows it. There is no re-render of a tree; the thing that read the value is what updates."});_e727.appendChild(_e728);const _e729=WF.el("h2",{className:"wf-heading",id:"state"},"state");_e727.appendChild(_e729);const _e730=Component_CodeBlock({code:"page Counter(path: \"/\") {\n    state count = 0                  // inferred: Number\n    state name: String = \"\"          // declared\n    state items: [String] = []       // a list of strings\n    state selected: String? = null   // may be null\n    state user = { name: \"Sam\", tags: [\"a\"] }   // a map literal: its fields are its type\n\n    Text(\"{name} has clicked {count} times\")\n    Button(\"+\") { on click { count = count + 1 } }\n}",title:"wf"});_e727.appendChild(_e730);const _e731=WF.el("div",{className:"wf-markdown",markdown:"A `state` is declared at the top of a page, component or store body, with an initial value. Its type is read off the value, or written after a colon; [chapter 10](/docs/guide/types) has the type language. Assigning a value of another type is an error (`T01`).\n\nState is written only in an imperative block — a handler, an action, an effect: `count = count + 1`, `user.name = \"Ann\"`, `items[0] = \"x\"`, or through a method that changes it in place (`items.push(\"x\")`). The static paint seeds every state with its initial value, so a pre-rendered page shows what the first paint would."});_e727.appendChild(_e731);const _e732=WF.el("h2",{className:"wf-heading",id:"derived"},"derived");_e727.appendChild(_e732);const _e733=WF.el("div",{className:"wf-markdown",markdown:"A derived value is computed from state and recomputed when any state it read changes:"});_e727.appendChild(_e733);const _e734=Component_CodeBlock({code:"page Cart(path: \"/\") {\n    state items = [{ name: \"Pen\", price: 2.5, qty: 4 }, { name: \"Pad\", price: 6, qty: 1 }]\n    state taxRate = 0.2\n    derived subtotal = items.reduce((sum, i) => sum + i.price * i.qty, 0)\n    derived total = subtotal * (1 + taxRate)\n    derived empty = items.length == 0\n\n    Text(\"Subtotal: {format(subtotal, .currency)}\")\n    Text(\"Total: {format(total, .currency)}\")\n    if empty { Text(\"Your cart is empty.\").muted }\n}",title:"wf"});_e727.appendChild(_e734);const _e735=WF.el("div",{className:"wf-markdown",markdown:"A derived value may read other derived values and call actions. It is never assigned to. It is seeded in the static paint from the initial state."});_e727.appendChild(_e735);const _e736=WF.el("h2",{className:"wf-heading",id:"effect"},"effect");_e727.appendChild(_e736);const _e737=WF.el("div",{className:"wf-markdown",markdown:"An effect runs once, and again whenever a state it read changes. Its `cleanup { }` block runs before the next run and when the page (or the branch the effect lives in) leaves:"});_e727.appendChild(_e737);const _e738=Component_CodeBlock({code:"page Watch(path: \"/\") {\n    state query = \"\"\n    state hits = 0\n    effect {\n        log(\"query is now {query}\")\n        hits = hits + 1\n    }\n    effect {\n        document.title = \"Search: {query}\"\n        cleanup { document.title = \"Search\" }\n    }\n    Input(bind: query, placeholder: \"Search\")\n    Text(\"{hits} runs\")\n}",title:"wf"});_e727.appendChild(_e738);const _e739=WF.el("div",{className:"wf-markdown",markdown:"Use effects for the world outside the page — the document title, a third-party widget, a subscription. Keep computation in `derived`."});_e727.appendChild(_e739);const _e740=WF.el("h2",{className:"wf-heading",id:"actions-and-locals"},"Actions and locals");_e727.appendChild(_e740);const _e741=WF.el("div",{className:"wf-markdown",markdown:"An action is a named function of the page, component or store. Its body is an imperative block: assignments, `let` locals, calls, `if`, `for`, `try`, `return`, `await`."});_e727.appendChild(_e741);const _e742=Component_CodeBlock({code:"page Todos(path: \"/\") {\n    state items: [String] = []\n    state draft = \"\"\n\n    action add() {\n        let text = draft.trim()\n        if text == \"\" { return }\n        items.push(text)\n        draft = \"\"\n    }\n\n    action remove(index: Number) {\n        items = items.filter((_, i) => i != index)\n    }\n\n    action load() {\n        let r = await fetch(\"/api/todos\")\n        let { todos } = r\n        items = todos\n    }\n\n    Input(bind: draft, placeholder: \"What needs doing?\") { on keydown(e) { if e.key == \"Enter\" { add() } } }\n    Button(\"Add\").primary { on click { add() } }\n    for item, i in items {\n        Row { Text(item)  IconButton(icon: \"trash\", label: \"Remove\") { on click { remove(i) } } }\n    }\n}",title:"wf"});_e727.appendChild(_e742);const _e743=WF.el("div",{className:"wf-markdown",markdown:"- `let x = …` declares a local of the action (or handler). `let { a, b } = m` and `let [x, y] = l` destructure.\n- `await` inside an action makes it asynchronous; `name.pending` is `true` while a call of it runs ([chapter 6](/docs/guide/events-and-forms#pending-actions)).\n- `for item in list { }`, `for item, i in list { }` and `for n in 1..=10 { }` loop; `try { } catch e { }` guards a call that may fail; `return x` leaves with a value.\n- A page's action is called by name, `add()`; a store's through the store, `Todos.add()`."});_e727.appendChild(_e743);const _e744=WF.el("h2",{className:"wf-heading",id:"persist"},"persist");_e727.appendChild(_e744);const _e745=WF.el("div",{className:"wf-markdown",markdown:"`persist` is a `state` kept in the browser's storage across visits. It is read back on the next visit and written on every change:"});_e727.appendChild(_e745);const _e746=Component_CodeBlock({code:"page Prefs(path: \"/\") {\n    persist theme = \"light\"\n    persist perPage = 20\n    Select(bind: theme, label: \"Theme\") {\n        Select.Option(\"Light\", value: \"light\")\n        Select.Option(\"Dark\", value: \"dark\")\n    }\n    Slider(bind: perPage, min: 10, max: 100, step: 10, label: \"Rows per page\")\n}",title:"wf"});_e727.appendChild(_e746);const _e747=WF.el("div",{className:"wf-markdown",markdown:"It works in a store too, where every page shares it. The key is the owner's name and the state's (`Prefs.theme`), so a page and a store may each have a `theme`. Storage that is blocked or full falls back to the initial value."});_e727.appendChild(_e747);const _e748=WF.el("h2",{className:"wf-heading",id:"timers"},"Timers");_e727.appendChild(_e748);const _e749=WF.el("div",{className:"wf-markdown",markdown:"`every(ms) { }` repeats; `after(ms) { }` fires once. Both stop when what declared them leaves the page — a branch that closes, a list item that is removed, a route that changes:"});_e727.appendChild(_e749);const _e750=Component_CodeBlock({code:"page Clock(path: \"/\") {\n    state seconds = 0\n    state toast: String? = \"Welcome\"\n    every(1000) { seconds = seconds + 1 }\n    after(3000) { toast = null }\n    Text(\"{seconds}s on this page\")\n    if let t = toast { Alert(t).info }\n}",title:"wf"});_e727.appendChild(_e750);const _e751=WF.el("h2",{className:"wf-heading",id:"ownership-what-leaves-leaves"},"Ownership: what leaves, leaves");_e727.appendChild(_e751);const _e752=WF.el("div",{className:"wf-markdown",markdown:"Everything a body creates — effects, timers, listeners, handles — belongs to the scope it was created in. When that scope's nodes leave (an `if` branch turns false, an item drops out of a `for`, a `match` arm changes, a route changes), the runtime disposes of them: intervals are cleared, effects fall silent, cleanups run. You never unsubscribe by hand."});_e727.appendChild(_e752);const _e753=WF.el("h2",{className:"wf-heading",id:"element-handles"},"Element handles");_e727.appendChild(_e753);const _e754=WF.el("div",{className:"wf-markdown",markdown:"`ref: name` on an element gives you a handle, read as the element itself:"});_e727.appendChild(_e754);const _e755=Component_CodeBlock({code:"page Search(path: \"/\") {\n    state q = \"\"\n    Input(bind: q, ref: box, placeholder: \"Search\")\n    Button(\"Focus the box\") { on click { box.focus() } }\n    Button(\"Clear\") { on click { q = \"\"  box.focus() } }\n    on key(\"/\") { box.focus() }\n}",title:"wf"});_e727.appendChild(_e755);const _e756=WF.el("div",{className:"wf-markdown",markdown:"The name is declared by its use; `box.value`, `box.scrollIntoView()`, anything the element has."});_e727.appendChild(_e756);const _e757=WF.el("h2",{className:"wf-heading",id:"the-browser-as-values"},"The browser as values");_e727.appendChild(_e757);const _e758=WF.el("div",{className:"wf-markdown",markdown:"Four names are always in scope, kept current by the runtime:"});_e727.appendChild(_e758);const _e759=Component_CodeBlock({code:"page Where(path: \"/\") {\n    Text(if viewport.md { \"wide\" } else { \"narrow\" })   // viewport.width, .height, .sm .md .lg .xl\n    derived tab = query.tab ?? \"all\"                      // the URL's ?tab=x\n    Text(\"Tab: {tab}\")\n    Text(\"Section: {hash}\")                               // the URL's #fragment\n    Text(\"Theme: {theme}\")                                // light, dark or system\n}",title:"wf"});_e727.appendChild(_e759);const _e760=WF.el("div",{className:"wf-markdown",markdown:"Breakpoints: `sm` 640px, `md` 768px, `lg` 1024px, `xl` 1280px. A `state` you declare with one of these names shadows it. In the static paint they are unknown, so a condition on them renders as its live branch once hydrated."});_e727.appendChild(_e760);const _e761=WF.el("h2",{className:"wf-heading",id:"where-values-come-from-in-order"},"Where values come from, in order");_e727.appendChild(_e761);const _e762=WF.el("div",{className:"wf-markdown",markdown:"1. Route parameters and props (`slug`, `_ label`).\n2. `state`, `persist`, `derived`, `resource`, actions of the body.\n3. A `use`d store's members, through the store's name (`Cart.total`).\n4. Loop variables and arm bindings inside their block.\n5. Constants and `data` files, `env.X`, the browser's values.\n6. Browser globals: `window`, `document`, `localStorage`, `JSON`, `Math`, `Date`, `fetch`, `setTimeout`, `console`, `navigator`, `location`."});_e727.appendChild(_e762);_root.appendChild(_e727);return _root;}
WF.page("StateAndReactivity",Page_StateAndReactivity);