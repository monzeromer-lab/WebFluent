function Page_OfflineGuide(params){const _root=document.createDocumentFragment();const _e2535=WF.el("div",{className:"wf-stack wf-gap--lg"});const _e2536=WF.el("div",{className:"wf-markdown",markdown:"**4.1.** Name `offline` in the config and the build writes a service worker that keeps the site usable with no network: stored pages load, writes made offline are sent when the connection returns, and a new deploy is offered to the reader rather than swapped in under them. There is no worker to write and no cache to name by hand."});_e2535.appendChild(_e2536);const _e2537=WF.el("h2",{className:"wf-heading",id:"turning-it-on"},"Turning it on");_e2535.appendChild(_e2537);const _e2538=WF.el("div",{className:"wf-markdown",markdown:"Name `offline` in `webfluent.app.json` and the build writes a service worker, `sw.js`, that stores the site for when the network is gone:"});_e2535.appendChild(_e2538);const _e2539=Component_CodeBlock({code:"{ \"offline\": {\n    \"precache\": [\"/\", \"/docs/*\"],\n    \"fallback\": \"/offline\",\n    \"cache\": { \"/api/*\": \"network-first\" },\n    \"sync\": true\n} }",title:"json"});_e2535.appendChild(_e2539);const _e2540=WF.el("table",{className:"wf-table"},WF.el("caption",{className:"wf-visually-hidden"},"Turning it on"));const _e2541=WF.el("thead",{});const _e2542=WF.el("tr",{});const _e2543=WF.el("th",{scope:"col"},"Key");_e2542.appendChild(_e2543);const _e2544=WF.el("th",{scope:"col"},"What it does");_e2542.appendChild(_e2544);_e2541.appendChild(_e2542);_e2540.appendChild(_e2541);const _e2545=WF.el("tbody",{});const _e2546=WF.el("tr",{});const _e2547=WF.el("td",{});const _e2548=WF.el("div",{className:"wf-markdown",markdown:"`precache`"});_e2547.appendChild(_e2548);_e2546.appendChild(_e2547);const _e2549=WF.el("td",{});const _e2550=WF.el("div",{className:"wf-markdown",markdown:"The routes a first visit stores, as globs — `\"/\"` by default. Each is stored with its own chunk and sheet, and the shell with them."});_e2549.appendChild(_e2550);_e2546.appendChild(_e2549);_e2545.appendChild(_e2546);const _e2551=WF.el("tr",{});const _e2552=WF.el("td",{});const _e2553=WF.el("div",{className:"wf-markdown",markdown:"`fallback`"});_e2552.appendChild(_e2553);_e2551.appendChild(_e2552);const _e2554=WF.el("td",{});const _e2555=WF.el("div",{className:"wf-markdown",markdown:"A page's `path`, shown for a route that was not stored."});_e2554.appendChild(_e2555);_e2551.appendChild(_e2554);_e2545.appendChild(_e2551);const _e2556=WF.el("tr",{});const _e2557=WF.el("td",{});const _e2558=WF.el("div",{className:"wf-markdown",markdown:"`cache`"});_e2557.appendChild(_e2558);_e2556.appendChild(_e2557);const _e2559=WF.el("td",{});const _e2560=WF.el("div",{className:"wf-markdown",markdown:"How a path the build did not write is fetched: `network-first`, `cache-first`, `stale-while-revalidate` or `network-only`. A path not named passes through."});_e2559.appendChild(_e2560);_e2556.appendChild(_e2559);_e2545.appendChild(_e2556);const _e2561=WF.el("tr",{});const _e2562=WF.el("td",{});const _e2563=WF.el("div",{className:"wf-markdown",markdown:"`sync`"});_e2562.appendChild(_e2563);_e2561.appendChild(_e2562);const _e2564=WF.el("td",{},"A write made with no network is kept and sent later (below).");_e2561.appendChild(_e2564);_e2545.appendChild(_e2561);_e2540.appendChild(_e2545);_e2535.appendChild(_e2540);const _e2565=WF.el("div",{className:"wf-markdown",markdown:"A stored route loads with no network at all. When there is one, a navigation still goes to it first, so a page that changed is never served stale while the server is there to ask."});_e2535.appendChild(_e2565);const _e2566=WF.el("h2",{className:"wf-heading",id:"a-write-made-offline"},"A write made offline");_e2535.appendChild(_e2566);const _e2567=WF.el("div",{className:"wf-markdown",markdown:"With `sync`, a write — any method but `GET` and `HEAD`, through `fetch` or an `api` — that fails because the network is gone is kept instead of thrown, and the call resolves with nothing, so what the page showed stays shown. The writes are sent in order when the connection returns: by Background Sync where the browser has it, so a closed tab still sends them, and by the page otherwise. A server that answers has the write, even if it refuses it; one that is down or asks to wait keeps it for later. A `File` body is not kept, and its call fails as before.\n\n`network.queued` is how many wait."});_e2535.appendChild(_e2567);const _e2568=WF.el("h2",{className:"wf-heading",id:"a-new-version"},"A new version");_e2535.appendChild(_e2568);const _e2569=WF.el("div",{className:"wf-markdown",markdown:"The worker is versioned by a hash of everything the build wrote, so any deploy that changes a byte is a new version. It installs beside the old one and waits for the page to take it:"});_e2535.appendChild(_e2569);const _e2570=Component_CodeBlock({code:"page Home(path: \"/\", title: \"Home\", description: \"The front page.\") {\n    if update.available {\n        Button(\"A new version is ready — reload\") { on click { update.apply() } }\n    }\n    if network.queued > 0 {\n        Text(\"{network.queued} change(s) will be sent when you are back online\").muted\n    }\n    Heading(\"Home\").h1\n}",title:"wf"});_e2535.appendChild(_e2570);const _e2571=WF.el("div",{className:"wf-markdown",markdown:"`update.apply()` takes it and reloads the page once: never on the first install, which only takes over a page that already works, and never twice for one update. The old version's store is deleted when the new one takes over.\n\nUnder `wf serve` the worker takes itself away: a page answered from its own store would hide every edit you make."});_e2535.appendChild(_e2571);const _e2572=WF.el("h2",{className:"wf-heading",id:"what-it-needs"},"What it needs");_e2535.appendChild(_e2572);const _e2573=WF.el("div",{className:"wf-markdown",markdown:"- **HTTPS**, or `localhost`. Browsers only run service workers from a secure origin.\n- **A host that serves `sw.js` fresh.** The page registers it with `updateViaCache: \"none\"`, so the browser asks for it on every visit — but a CDN in front can still hold a copy. Give `sw.js` `Cache-Control: no-cache` ([Deploying](/docs/guide/deploying#cache-headers)).\n- **Everything in `build/` deployed together.** The worker's version is a hash of every file the build wrote; deploying half a build confuses it."});_e2535.appendChild(_e2573);const _e2574=WF.el("h2",{className:"wf-heading",id:"trying-it"},"Trying it");_e2535.appendChild(_e2574);const _e2575=WF.el("div",{className:"wf-markdown",markdown:"1. `wf build`, then serve `build/` with anything static — `python3 -m http.server -d build` will do. (`wf serve` removes the worker on purpose.)\n2. Open the site, then in the browser's developer tools, *Application → Service workers*, check it is activated.\n3. Tick *Offline* in the *Network* panel and reload: stored routes load, the rest show the `fallback`.\n4. Make a write, untick *Offline*, and watch it go out.\n\n`wf verify` runs against the built output too, and the repository's own browser tests hold the whole flow to a real Chrome."});_e2535.appendChild(_e2575);const _e2576=WF.el("h2",{className:"wf-heading",id:"limits"},"Limits");_e2535.appendChild(_e2576);const _e2577=WF.el("div",{className:"wf-markdown",markdown:"- A `File` body is never kept: an upload made offline fails as before.\n- A kept write is sent once it can be, possibly minutes later — make the server's endpoints safe to receive late, and idempotent where a retry could repeat one.\n- What `cache` does not name passes straight through, so an API the site depends on while offline needs a `network-first` or `stale-while-revalidate` entry.\n- The browser decides how much it keeps; a store can be evicted under storage pressure, and the next visit fetches it again."});_e2535.appendChild(_e2577);_root.appendChild(_e2535);return _root;}
WF.page("OfflineGuide",Page_OfflineGuide);