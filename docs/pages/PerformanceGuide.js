function Page_PerformanceGuide(params){const _root=document.createDocumentFragment();const _e3107=WF.el("div",{className:"wf-stack wf-gap--lg"});const _e3108=WF.el("h2",{className:"wf-heading",id:"what-the-build-does-already"},"What the build does already");_e3107.appendChild(_e3108);const _e3109=WF.el("div",{className:"wf-markdown",markdown:"- **Only the runtime you use.** The runtime is a set of modules; a build keeps the ones its code reaches. A page of static text carries 3 of its 37 modules: an `app.js` of 11 kB, 3.7 kB gzipped.\n- **A chunk per page.** `build.split` (on by default) writes each page's code and styles as their own files, loaded when its route is first shown.\n- **Pre-rendered HTML** with `build.ssg`: the first paint needs no script.\n- **One pruned stylesheet.** Only the built-ins you use, only the tokens something names.\n- **Precompressed.** A `.gz` beside every text file, for a host that serves it; `wf serve` sends the same bytes, so Lighthouse against the dev server measures what a deploy sends.\n- **Images at every width**, with `width`/`height` so nothing shifts, a placeholder while they load, the first one eager and the rest lazy ([Media](/docs/guide/media)).\n- **Minified** output (`build.minify`)."});_e3107.appendChild(_e3109);const _e3110=WF.el("h2",{className:"wf-heading",id:"seeing-what-it-weighs"},"Seeing what it weighs");_e3107.appendChild(_e3110);const _e3111=Component_Terminal({lines:[({command:"wf build --stats",output:""})]});_e3107.appendChild(_e3111);const _e3112=Component_CodeBlock({code:"  What it weighs\n    app.js                               47.6 kB  (14.6 kB gzipped)  +20 B\n    styles.css                           16.9 kB  (3.7 kB gzipped)\n    index.html                            5.1 kB  (1.3 kB gzipped)\n    pages/Suggest.js                      2.2 kB  (0.8 kB gzipped)\n    pages/Home.js                         1.4 kB  (0.6 kB gzipped)\n    …\n    total                                90.7 kB\n\n  Runtime: 14 of 37 modules, 89.3 kB of 220.3 kB (before minifying)\n    core          19.6 kB  always\n    motion        16.6 kB  needed by when, match, each, router\n    form          12.1 kB  WF.form\n    router        10.7 kB  WF.router\n    store          6.5 kB  WF.store\n    …\n    left out: helpers format offline peer scalars text slot show browser …\n\n  Tokens: 26 nothing in the output names, left out",title:"text"});_e3107.appendChild(_e3112);const _e3113=WF.el("div",{className:"wf-markdown",markdown:"That is the [tutorial](/docs/guide/tutorial)'s site: a form brings `form` and `field`, a store `store` and `keep`, a second page `router`.\n\nEach build records its sizes in `.wf-sizes.json`, so the next `--stats` shows what moved. Each runtime module is listed with what reached it — the first place to look when a module you did not expect is in."});_e3107.appendChild(_e3113);const _e3114=WF.el("h2",{className:"wf-heading",id:"budgets"},"Budgets");_e3107.appendChild(_e3114);const _e3115=Component_CodeBlock({code:"{ \"build\": { \"budget\": { \"app.js\": \"40 kB\", \"styles.css\": \"30 kB\", \"pages/Home.js\": \"10 kB\" } } }",title:"json"});_e3107.appendChild(_e3115);const _e3116=WF.el("div",{className:"wf-markdown",markdown:"A gzipped size an output should stay under. Going over prints a warning and the build goes on; a CI job that wants to fail on it can read the same numbers."});_e3107.appendChild(_e3116);const _e3117=WF.el("h2",{className:"wf-heading",id:"timing-it-in-a-browser"},"Timing it in a browser");_e3107.appendChild(_e3117);const _e3118=Component_Terminal({lines:[({command:"wf build && wf verify --budget 800",output:""})]});_e3107.appendChild(_e3118);const _e3119=WF.el("div",{className:"wf-markdown",markdown:"`wf verify` loads every route in headless Chrome and prints its first contentful paint, the nodes in the document and the bytes and requests it took; `--budget` fails a route slower than that. Lighthouse and the browser's performance panel work on the build as on any site."});_e3107.appendChild(_e3119);const _e3120=WF.el("h2",{className:"wf-heading",id:"what-makes-a-site-slow"},"What makes a site slow");_e3107.appendChild(_e3120);const _e3121=WF.el("div",{className:"wf-markdown",markdown:"- **A large `data` file.** `data posts = \"posts.json\"` is inlined into the bundle, so a 2 MB file is 2 MB of JavaScript. Keep build-time data to what pages show; fetch the rest, or split it across pages that each read one.\n- **A waterfall of fetches.** A resource whose URL depends on another's answer waits for it. Where you can, ask for both at once, or have the server send them together.\n- **Layout decided by `viewport`.** It is unknown until the script runs, so the page paints one branch and swaps. Use responsive values, which are CSS.\n- **`runtime: \"full\"`.** It ships every module. Only needed when a hand-written script calls `WF` in ways the build cannot see.\n- **Web fonts.** Each weight is a download. Use `display=swap`, few weights, or the baseline's system fonts.\n- **Unkeyed lists that change.** `for item in items` rebuilds the list; `for item in items by item.id` moves the nodes that stayed.\n- **Pictures from `public/`.** They are copied as they are. Declare them with `image` to get every width and a placeholder."});_e3107.appendChild(_e3121);_root.appendChild(_e3107);return _root;}
WF.page("PerformanceGuide",Page_PerformanceGuide);