component StatCard(title: String, value: Number, color: String) {
    Card.elevated.fadeIn {
        style {
            borderLeft: 4px solid $color-primary
        }
        Card.Body {
            Text(title).muted.sm
            Heading(value).h2
        }
    }
}